# 后端 API 配置
API_BASE_URL=https://anyrouter.top

# System Prompt 配置
SYSTEM_PROMPT_REPLACEMENT="You are Claude Code, Anthropic's official CLI for Claude."

# System Prompt 插入模式（可选）
# 设置为 true/1/yes 时，如果 system[0].text 不包含 "Claude Code"（忽略大小写），则在开头插入新元素而非替换
# 设置为 false 或不设置时，总是替换 system[0].text
SYSTEM_PROMPT_BLOCK_INSERT_IF_NOT_EXIST=false

# 如果需要通过代理访问，请取消下面两行的注释并设置代理地址
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890

# 调试模式开关：设置为 true 启用调试日志输出（仅测试环境使用）
DEBUG_MODE=false

# 服务端口配置：默认 8088
PORT=8088

# ==========================================
# Web 管理面板配置
# ==========================================

# 启用管理面板
ENABLE_DASHBOARD=true
# 管理面板 API Key（用于保护管理面板）
# 请修改为强密码，格式：Bearer Token 可以是任意字符串
# DASHBOARD_API_KEY=your-secret-api-key-change-me-please

# 生产环境建议使用更强的密码：
# DASHBOARD_API_KEY=$(openssl rand -hex 32)  # Linux/macOS
# DASHBOARD_API_KEY=[Random-PowerShell-Command]  # Windows PowerShell
