# TODO
这是一个 TODO List.

## 功能开发 TODO

- [x] 优化支持 PWA
- [x] 请求列表项的左边 Dot 在请求进行中时为灰色，且有呼吸动画效果，请求结束再按原来的根据请求码显示着色
- [ ] 请求列表貌似数据不全
- [ ] 支持配置多个端点，并且支持端点测试可用性，API_BASE_URL -> API_BASE_URLS，若 API_BASE_URL 存在，则加入 API_BASE_URLS
- [ ] 新增请求响应时间趋势
- [ ] 仪表板的数据和监控中心的请求数据也存到 sqlite，但不能存到 running_log 里，要另外存到一个 request_log 数据库文件
- [ ] 请求数据要定期清理，每天只存最新的 1000 条请求数据
- [x] 配置管理页面改版，允许自定义配置
    - 保存后提示自动重启后端服务
    - 部署端口不可修改，也不用显示出来
    - 配置保存在数据库，移除 .env，初始化数据也在数据库